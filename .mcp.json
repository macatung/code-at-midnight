{
  "mcpServers": {
    "task-hub": {
      "type": "http",
      "url": "${TASK_HUB_URL:-http://localhost:8000}/api/tasks/mcp",
      "headers": {
        "Authorization": "Bearer ${TASK_HUB_PROJECT_MCP_TOKEN}",
        "X-Task-Hub-Project": "${TASK_HUB_PROJECT_ID}"
      }
    }
  }
}
