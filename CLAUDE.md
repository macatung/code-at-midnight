# Macatung workflow

Use the assigned work item as the implementation contract. Read its acceptance criteria, risk level and definition of done before editing.

Keep the implementation scoped to the macatung.dev portfolio. Never merge, deploy, expose secrets or perform destructive operations without human approval.

Run relevant tests and report the exact commands and results before requesting review.
