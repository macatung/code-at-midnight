# Task Hub workflow

Use the assigned Task Hub work item as the implementation contract. Read its acceptance criteria, risk level and definition of done before editing.

When available, use the Task Hub MCP server to read the work item, start/update the agent run and attach verification evidence. Keep lifecycle status accurate. Never merge, deploy, expose secrets or perform destructive operations without human approval.

Run relevant tests and report the exact commands and results before requesting review.
